# 작업내역-W009

## 1. 작업 식별 정보
- 작업 ID: WIN-009
- 작업명: 1단계 회귀 테스트 세트 정리
- 기준 문서: change-winapp-phase1-tickets.md (WIN-009)
- 작업 일자: 2026-05-22
- 작업 브랜치: webapp

## 2. 작업 목표
WIN-009 목표에 맞춰 코어 분리 이후 회귀를 빠르게 감지할 최소 테스트 묶음을 정리하고,
실행 명령을 표준화해 문서에 반영한다.

핵심 요구사항:
- CLI 회귀 스모크 5개 선정
- IPC 계약 테스트 최소 1개 추가
- 테스트 실행 명령 README 반영

## 3. 변경 파일
- 수정: package.json
- 신규: desktop/__tests__/ipc-contract.test.ts
- 수정: README.md
- 수정: 한글해석-분석본/작업문서/change-winapp-phase1-tickets.md
- 신규: 한글해석-분석본/작업문서/작업내역폴더/작업내역-W009.md

## 4. 실제 변경 내역

### 4.1 회귀 테스트 실행 스크립트 정리
`package.json`에 WIN-009용 실행 스크립트를 추가했다.

추가 스크립트:
- `test:phase1:cli-smoke:compiled`
  - CLI 스모크 5개 고정 실행
  - 대상: `dist/cli/__tests__/...` 5개
- `test:phase1:ipc-contract`
  - `build:desktop` 후 IPC 계약 테스트 실행
- `test:phase1:regression`
  - CLI 스모크 + IPC 계약 테스트 통합 실행 엔트리

### 4.2 IPC 계약 테스트 추가
`desktop/__tests__/ipc-contract.test.ts`를 신규 생성했다.

검증 항목:
1) 허용 명령 성공
- 입력: `state_get_status`
- 기대: `ok: true`, `status: ok`, `runtime: desktop-main`

2) 비허용 명령 차단
- 입력: `state_get_status_invalid`
- 기대: `ok: false`, `error.code: UNKNOWN_COMMAND`

3) 잘못된 페이로드 차단
- 입력: 빈 command
- 기대: `ok: false`, `error.code: INVALID_REQUEST`

### 4.3 README 실행 명령 반영
`README.md`에 `Phase-1 regression commands (WIN-009)` 섹션을 추가했다.

반영 명령:
- `npm run test:phase1:cli-smoke:compiled`
- `npm run test:phase1:ipc-contract`
- `npm run test:phase1:regression`

### 4.4 기준 문서 동기화
`change-winapp-phase1-tickets.md` WIN-009 섹션 동기화:
- [x] CLI 스모크 테스트 셋 확정
- [x] IPC 계약 테스트 추가
- [x] 테스트 실행 명령 README 반영

Taiga 식별자 반영:
- User Story ID: 23
- Task IDs: 55, 56, 57

## 5. 검증 내역

### 5.1 IPC 계약 테스트 실행
실행 명령:
- `npm run test:phase1:ipc-contract`

결과:
- 성공
- 3 tests, 0 failures

### 5.2 CLI 스모크 실행
실행 명령:
- `npm run test:phase1:cli-smoke:compiled`

결과:
- 실패
- 16 failures
- 주요 원인:
  - Windows 실행 환경에서 tmux/psmux 전제와 충돌
  - 일부 테스트가 `powershell.exe`, `codex`, `git` 외부 실행 파일 의존

### 5.3 통합 엔트리 실행
실행 명령:
- `npm run test:phase1:regression`

결과:
- 실패
- 첫 실패 예시:
  - `codex-plugin-layout.test.js`에서 `AssertionError (null !== 0)`
- 추가 실패 예시:
  - `spawnSync powershell.exe ENOENT`
  - `spawnSync codex ENOENT`

## 6. WIN-009 체크리스트 반영 상태
- [x] CLI 스모크 테스트 셋 확정
- [x] IPC 계약 테스트 추가
- [x] 테스트 실행 명령 README 반영

## 7. 리스크 및 이슈
1) WIN-009 DoD(로컬에서 일관되게 통과) 부분 미충족
- 테스트 세트와 실행 명령은 정리 완료했으나,
  현재 Windows 환경에서는 CLI 스모크 일부가 외부 실행파일/런타임 가정으로 실패

2) 환경 의존성
- `tmux/psmux`, `powershell.exe`, `codex`, `git` 존재 여부에 따라 결과가 달라짐

## 8. 다음 권장 액션
1. CLI 스모크 5개를 환경 독립 케이스로 교체
- 외부 바이너리 의존 테스트를 단계적으로 제외/분리
- 순수 로직 테스트 중심으로 재구성

2. 플랫폼별 스모크 분리
- `test:phase1:cli-smoke:windows`
- `test:phase1:cli-smoke:unix`

3. WIN-010 진행 전 게이트 정책 명시
- 회귀 테스트의 필수/권장 범위를 OS별로 분리 정의
