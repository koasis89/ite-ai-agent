# PR 초안: 문제 #715 수정 team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 두뇌/역할 분할

## 대상 지점
`dev`

## 요약
이 후속 PR은 team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 모드 실행 기본값을 일반 실행기 파트에서 분리하여 문제 #715를 수정합니다.

이 PR을 통해 #715를 수정할 계획입니다.

## 변경 사항
- 감독된 기본 team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 실행을 위한 `team-executor` 역할 프롬프트를 추가하세요.
- 카탈로그/설정 경로에 `team-executor`을 등록하면 실제 team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 실행을 위해 프롬프트와 기본 에이전트(Agent)가 실제로 설치 가능합니다.
- 명시적인 `N:agent-type` team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 출시를 변경하지 않고 유지하세요.
- 보수적인 팬아웃 가드를 추가하여 약하게 구조화된 소규모 team(멀티 worker(실제작업을 수행하는 실행단위) 인터페이스 실행 오케스트레이션) 작업이 기본적으로 과도하게 분할되지 않도록 합니다.
- 역할 발견 및 분해 동작에 대한 집중 회귀 테스트 추가

## 확인
- [x] `npm run lint`
- [x] `npm test`
