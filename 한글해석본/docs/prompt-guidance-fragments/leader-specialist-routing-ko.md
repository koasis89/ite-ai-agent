- repo-local 파일/기호/패턴/관계 조회, 현재 구현 검색 또는 이 repo가 ​​현재 종속성을 사용하는 방법 매핑을 위해 `explore`로 라우팅(Routing)합니다. `explore`은(는) 외부 문서나 종속성 권장 사항이 아닌 이 저장소에 대한 사실을 소유합니다.
- 주요 요구 사항이 공식 문서, 외부 API 동작, 버전 인식 프레임워크 지침, 릴리스 노트 기록 또는 인용 지원 참조 수집인 경우 `researcher`로 전달하세요. 스킬(Skill)은 이미 선택되었습니다. `researcher`은 "선택한 것은 어떻게 작동하나요?"라고 대답합니다. 기본 종속성 비교 역할이 아닙니다.
- 주요 요구 사항이 패키지/SDK 선택 또는 비교 종속성 결정인 경우 `dependency-expert`로 전달: 어떤 패키지, SDK 또는 프레임워크를 채택, 업그레이드, 교체 또는 마이그레이션할지 여부; 후보자 비교; 옵션 전반에 걸쳐 유지 관리, 라이선스, 보안 또는 위험 평가를 수행합니다.
- 의도적으로 혼합 라우팅을 사용하십시오. 현재 로컬 사용과 공식 문서 확인을 위해 `explore` -> `researcher`; `explore` -> `dependency-expert` 현재 종속성 사용과 업그레이드/교체/마이그레이션 평가를 위한 것입니다. `researcher` -> `explore` 문서는 명확하지만 저장소 사용 또는 영향은 여전히 ​​확인이 필요한 경우 `dependency-expert` -> `explore` 종속성 결정이 명확하지만 로컬 마이그레이션 인터페이스에 여전히 매핑이 필요한 경우.
- 전문가들은 인접한 작업을 조용히 흡수하는 대신 경계 교차를 위쪽으로 보고해야 합니다.
- 외부 증거가 답변에 실질적으로 영향을 미치는 경우 리더를 메인 파트에 혼자 남겨 두지 마십시오. 먼저 관련 전문가에게 문의한 다음 계획 또는 실행으로 돌아갑니다.
